{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div>

        {% for user in users %}
            <p>Donc {{ user.firstname }} {{ user.lastname }}
                est née le {{ user.birthday |date("m/d/Y") }}
                et son email est {{ user.email }} <br></p>

            <a href="
{{ path('user_remove', {'id': user.id}) }}
">supprimer</a>
        {% endfor %}
    </div>
{% endblock %}
